local PremiumUsers = ({
    ["Aj_tzy27"] = true,
    ["Josefut47"] = true
})

if game.Players.LocalPlayer is not PremiumUsers then
    game.Players.LocalPlayer:Kick("Your not a premium user, contact the developer to be added to the lists.")
end

but if game.Players.LocalPlayer is PremiumUsers then
    Notify("Welcome back, " .. game.Players.LocalPlayer.Name .. "!")
end

if Notify is done then
    Notify("You have been verified as a premium user, enjoy the script!")
end

local Version = "Invalid"

if Version == "Invalid" then
    Notify("Your version of the script is Invalid!")
 then
    task.wait(2)
     then
        game.Players.LocalPlayer:Kick("Please Update First Before Using The Script, or Join our Discord Server in Order to get the Latest Loadstring.")
     end)
    end